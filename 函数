Add函数
int Add(int x,int y)
{
    int z=0;
    z=x+y;
    return z;
}
int main()
{
    int a=0;
    int b=0;
    printf("请输入a和b的值:");
    scanf("%d,%d",&a,&b);
    int sum=Add(a,b);
    printf("%d\n",sum);
    system("pause");
    return 0;
}
