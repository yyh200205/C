//分数求和
#include<stdio.h>
#include<stdlib.h>
int main()
{
    int i=0;
    double sum=0;
    for(i=1;i<=100;i++)
    {
        if(i%2==1)
        {
            sum=sum+1.0/i;
        }
        else
        {
            sum=sum-1.0/i;
        }
    }
    printf("%lf\n",sum);
    system("pause");
    return 0;
}

//求出10个数中最大的数
int main()
{
    int i=0;
    int max=0;
    int a=0;
    for(i=0;i<10;i++)
    {
        scanf("%d",&a);
        if(max<=a)
        {
            max=a;
        }
    }
    printf("%d\n",max);
    system("pause");
    return 0;
}
但是这个会出现问题，如果十个数都没有0大就会出现最大值是0的结果，所以可以用数组，然后把数组的第一个值赋给max
